<!DOCTYPE html>
<html lang="ko">
<head>
<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-5112383847606285"
     crossorigin="anonymous"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>유튜브 모아보기 - RANKING AI</title>
    <style>
        body { background: #0b0c0d; color: #fff; font-family: 'Pretendard', sans-serif; margin: 0; padding-bottom: 100px; }
        a { text-decoration: none; color: inherit; }

        /* 네비게이션 & 헤더 (메인과 동일) */
        .main-header { background: #121314; border-bottom: 1px solid #222; position: sticky; top: 0; z-index: 100; }
        .header-top { display: flex; justify-content: space-between; align-items: center; padding: 15px 40px; border-bottom: 1px solid #1a1b1c; }
        .logo { font-size: 28px; font-weight: 900; color: #ff5722; letter-spacing: -1px; } 
        .auth-buttons { display: flex; gap: 20px; align-items: center; }
        .btn-login { color: #bbb; font-size: 14px; }
        .btn-signup { background: #ff5722; color: #000; padding: 8px 20px; border-radius: 20px; font-weight: bold; font-size: 14px; }
        .header-nav { display: flex; justify-content: center; gap: 40px; padding: 12px 0; background: #0b0c0d; }
        .nav-item { color: #aaa; font-weight: 600; font-size: 16px; transition: 0.2s; position: relative; }
        .nav-item:hover, .nav-item.active { color: #ff5722; }
        .nav-item.active::after { content: ''; position: absolute; bottom: -12px; left: 0; width: 100%; height: 3px; background: #ff5722; }

        .container { max-width: 1200px; margin: 0 auto; padding: 30px 20px; }
        .upload-section { background: #161718; padding: 30px; border-radius: 15px; margin-bottom: 40px; border: 1px solid #333; }
        .input-row { display: flex; gap: 15px; margin-bottom: 15px; }
        .input-row input { flex: 1; background: #222; border: 1px solid #333; color: #fff; padding: 12px; border-radius: 8px; }
        .btn-upload { background: #ff5722; color: #000; border: none; padding: 12px; border-radius: 8px; cursor: pointer; font-weight: bold; width: 100%; }

        /* 🌟 반응형 유튜브 비디오 그리드 🌟 */
        .video-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); /* PC에선 여러 줄, 모바일에선 1줄 */
            gap: 25px;
        }
        .video-card {
            background: #161718; border: 1px solid #222; border-radius: 12px; overflow: hidden; transition: 0.2s;
        }
        .video-card:hover { border-color: #ff5722; transform: translateY(-5px); }
        
        /* 🌟 이 부분이 핵심! 비율 고정 마법 🌟 */
        .video-iframe-wrap {
            position: relative;
            width: 100%;
            aspect-ratio: 16 / 9; /* 가로 16 : 세로 9 꽉 차게! */
            background: #000;
        }
        .video-iframe-wrap iframe {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%; border: none;
        }
        
        .video-info { padding: 15px; }
        .video-title { font-size: 16px; font-weight: bold; margin: 0 0 10px 0; color: #fff; line-height: 1.4; }
        .btn-delete { background: none; border: none; color: #ff4a4a; cursor: pointer; font-size: 13px; font-weight: bold; padding: 0; }

        @media (max-width: 768px) {
            .header-top { flex-direction: column; gap: 15px; }
            .header-nav { justify-content: flex-start; overflow-x: auto; white-space: nowrap; padding: 12px 15px; gap: 20px; }
            .header-nav::-webkit-scrollbar { display: none; }
            .input-row { flex-direction: column; }
            .video-grid { grid-template-columns: 1fr; } /* 모바일에서는 무조건 꽉 찬 1줄로! */
        }
    </style>
</head>
<body>
    <header class="main-header">
        <div class="header-top">
            <img src="/logo.png" alt="RANKING AI 로고" style="height: 45px; cursor: pointer; object-fit: contain;" onclick="window.location.href='/?sort=views'">
            <div class="auth-buttons">
                <% if (user) { %>
                    <span style="color: #aaa; font-size: 14px;"><%= user.id %>님</span>
                    <a href="/logout" class="btn-login">로그아웃</a>
                <% } else { %>
                    <a href="/login" class="btn-login">로그인</a>
                    <a href="/signup" class="btn-signup">회원가입</a>
                <% } %>
            </div>
        </div>
        <div class="header-nav">
            <a href="/?sort=views" class="nav-item">RANKING AI 챠트</a>
            <a href="/youtube" class="nav-item active">유튜브</a>
            <a href="/shorts" class="nav-item">쇼츠</a>
            <a href="/board" class="nav-item">커뮤니티</a>
        </div>
    </header>

    <div class="container">
        <% if (user) { %>
        <div class="upload-section">
            <h3 style="margin-top:0; color:#ff5722;">📺 유튜브 영상 등록</h3>
            <form action="/add-youtube" method="POST">
                <div class="input-row">
                    <input type="text" name="title" placeholder="영상 제목을 입력하세요" required>
                    <input type="text" name="youtubeLink" placeholder="유튜브 링크 (https://youtube.com/...)" required>
                </div>
                <button type="submit" class="btn-upload">등록하기</button>
            </form>
        </div>
        <% } %>

        <h2 style="border-bottom: 2px solid #333; padding-bottom: 10px;">🔥 최신 유튜브 영상</h2>
        
        <div class="video-grid">
            <% 
               function getYoutubeId(url) {
                   const match = url.match(/(?:youtu\.be\/|youtube\.com\/(?:embed\/|v\/|watch\?v=|watch\?.+&v=))([^"&?\/\s]{11})/);
                   return match ? match[1] : null;
               }
            %>
            <% videos.forEach(function(video) { 
                const videoId = getYoutubeId(video.url);
            %>
            <div class="video-card">
                <div class="video-iframe-wrap">
                    <% if (videoId) { %>
                        <iframe src="https://www.youtube.com/embed/<%= videoId %>" allowfullscreen></iframe>
                    <% } else { %>
                        <div style="padding: 20px; text-align: center; color: #ff4a4a;">잘못된 링크입니다.</div>
                    <% } %>
                </div>
                <div class="video-info">
                    <h4 class="video-title"><%= video.title %></h4>
                    <div style="display: flex; justify-content: space-between; align-items: center;">
                        <span style="color: #888; font-size: 13px;">올린이: <%= video.uploader %></span>
                        <% if (user && (user.role === 'admin' || user.id === video.uploader)) { %>
                            <form action="/delete-video/<%= video._id %>" method="POST" style="margin: 0;" onsubmit="return confirm('삭제할까요?');">
                                <button type="submit" class="btn-delete">❌ 삭제</button>
                            </form>
                        <% } %>
                    </div>
                </div>
            </div>
            <% }); %>
            <% if (videos.length === 0) { %>
                <p style="color: #777;">등록된 유튜브 영상이 없습니다.</p>
            <% } %>
        </div>
    </div>
</body>
</html>
